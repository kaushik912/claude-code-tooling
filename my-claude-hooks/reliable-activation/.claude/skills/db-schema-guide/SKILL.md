---
name: db-schema-guide
description: Use when designing or querying database tables — enforces UUID PKs, created_at/updated_at timestamps, and FK indexes.
---

# DB Schema Skill

When activated:
- Always enforce UUIDs for primary keys.
- Always include `created_at` and `updated_at` timestamps.
- Suggest appropriate indexes for foreign key relationships.
